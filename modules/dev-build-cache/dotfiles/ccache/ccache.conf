# Host-wide ccache for Android NDK/CMake builds (React Native honours ccache when installed)
max_size = 25G
compression = true
# React Native builds under .cxx/Debug/<hash>/ which changes after `gradlew clean`;
# without these, every clean build misses (seen: 0/239 hits).
base_dir = /home/dylan
hash_dir = false

[Unit]
Description=Apply devbox DP/MST monitor layout dynamically
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/devbox-display-layout
Restart=always
RestartSec=5

[Install]
WantedBy=graphical-session.target

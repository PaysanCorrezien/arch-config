# Docker module
# Installs Docker and enables the service with nvidia-container-runtime support.

description: Docker engine with nvidia-container-runtime for GPU containers

post_install_hook: scripts/setup-docker.sh
hook_behavior: ask

# Start Docker after the media disk is mounted so SFTPGo never serves (and
# writes into) an empty /mnt/media8 on the root disk. Wants, not Requires:
# a missing disk must not take down every other container.
[Unit]
WantsMountsFor=/mnt/media8

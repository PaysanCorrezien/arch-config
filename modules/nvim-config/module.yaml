# Neovim Config Module
# Symlinks the neovim config from external/nvim-config submodule

description: Neovim configuration from external git submodule

# Symlink the entire submodule to ~/.config/nvim
dotfiles:
  - source: ../../external/nvim-config
    target: ~/.config/nvim

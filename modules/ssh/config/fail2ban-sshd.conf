# fail2ban jail config for SSH
# Deployed to /etc/fail2ban/jail.local
# Managed by arch-config SSH module

[DEFAULT]
bantime = 1h
findtime = 10m
maxretry = 5
banaction = ufw

[sshd]
enabled = true
port = 22
filter = sshd
backend = systemd
maxretry = 3
bantime = 24h

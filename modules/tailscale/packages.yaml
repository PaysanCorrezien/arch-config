# Tailscale VPN packages

description: Tailscale VPN and secure mesh networking

packages:
  - tailscale    # Tailscale VPN client and daemon
  - openresolv   # resolvconf implementation for MagicDNS (replaces systemd-resolvconf)
